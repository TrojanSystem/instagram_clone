import 'package:flutter/material.dart';
class InstaNewStoryScreen extends StatelessWidget {
  const InstaNewStoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: Center(
        child: Text('New Story Screen'),
      ),
    );
  }
}
