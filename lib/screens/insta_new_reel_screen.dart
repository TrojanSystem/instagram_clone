import 'package:flutter/material.dart';
class InstaNewReelScreen extends StatelessWidget {
  const InstaNewReelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: Center(
        child: Text('New Reel Screen'),
      ),
    );
  }
}
