import 'package:flutter/material.dart';

class InstaSplash extends StatelessWidget {
  const InstaSplash({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
