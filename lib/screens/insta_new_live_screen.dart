import 'package:flutter/material.dart';

class InstaNewLiveScreen extends StatelessWidget {
  const InstaNewLiveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('New Live Screen'),
      ),
    );
  }
}
